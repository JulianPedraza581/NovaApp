name: Django Tests with Coverage and Allure

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Descargar el repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configurar Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"

      # 3️⃣ Instalar dependencias + coverage + allure-pytest
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r NovaBack/requirements.txt
          pip install coverage allure-pytest

      # 4️⃣ Migraciones Django
      - name: Run migrations
        working-directory: ./NovaBack
        run: |
          python manage.py migrate

      # 5️⃣ Ejecutar tests con pytest + Allure
      - name: Run Django tests with Allure
        working-directory: ./NovaBack
        run: |
          mkdir -p tests/results/allure_raw
          pytest NovaBack --alluredir=tests/results/allure_raw
          coverage run manage.py test
          coverage xml -o tests/results/coverage.xml

      # 6️⃣ Generar reporte Allure
      - name: Generate Allure Report
        working-directory: ./NovaBack
        run: |
          mkdir -p tests/results/allure
          allure generate tests/results/allure_raw -o tests/results/allure --clean

      # 7️⃣ Subir artifacts
      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: NovaBack/tests/results/coverage.xml

      - name: Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: NovaBack/tests/results/allure
